{% extends 'base.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
<div class="container">

    <h1>{{ actor.name }}</h1>
    <div class="table-responsive-lxl">
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ actor.name }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <a href="{{ path('actor_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('actor_edit', {'id': actor.id}) }}" class="btn btn-dark">edit</a>

    {{ include('actor/_delete_form.html.twig') }}
</div>
<div class="table-responsive-lxl">

    {% endblock %}
