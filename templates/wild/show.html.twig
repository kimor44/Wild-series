{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">

        <h1 class="text-secondary">{{ slug }}</h1>
        <p><a href="{{ path('wild_show', {'slug':5}) }}">un lien</a></p>
    </div>
{% endblock %}